{
  "name": "expense-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
   "client": "npm run dev --prefix frontend/expense-tracker",
    "server": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}